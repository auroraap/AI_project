


def gradient_descent(doctor_location, patient_list, graph):
    # while patients in patient list
        # calculate total distance to all unvisited patients
        # for neighbors in neighbor list    
            # calculate total distance to all unvisited patients
        # choose the neighbor that has the lowest distance
        # store node in list of visited nodes (until a patient is reached, then this list is emptied)
        # store step in solution:
        #   doctor_step = {"location": doctor_location, "step length": doctor_step_length}
    return 0 # solution