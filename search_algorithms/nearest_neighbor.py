

def nearest_neighbor_search(doctor_location, patient_list, remaining_patients = {}):
    # while patient list is not empty
        # if a neighboring city is a patient, go there
        # else
        #   find nearest patient
        #   go to neighboring city with shortest distance estimate to patient
        # store step in solution:
        #   doctor_step = {"location": doctor_location, "step length": doctor_step_length}
        # update doctor location
    # return solution
    return 0